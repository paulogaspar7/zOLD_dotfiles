#!/usr/bin/env bash

cite about-alias
about-alias 'aliases for Firebird commands - depends on firebird plugin availability'

# Firebird Commands
alias fbstart="fb.start"
alias fbstop="fb.stop"
alias fbrestart="fb.restart"
