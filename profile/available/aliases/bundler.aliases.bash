#!/usr/bin/env bash

cite about-alias
about-alias 'aliases for Bundler commands'

# Bundler Commands
alias be="bundle exec"
alias bi="bundle install"
alias bl="bundle list"
alias bu="bundle update"
alias bp="bundle package"
